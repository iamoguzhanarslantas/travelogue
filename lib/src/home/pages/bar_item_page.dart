import 'package:flutter/material.dart';

class BarItemPage extends StatelessWidget {
  static const routeName = '/barItem';
  const BarItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bar Item Page'),
    );
  }
}
